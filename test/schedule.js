var assert = require("assert")
describe('Schedule', function(){
  describe('Create', function(){
    it('', function(done){
      done()
    })
    it('should not yet be published', function(done){
      done()
    })
  })
  describe('Publishing', function(){
    it('should publish the schedule', function(done){
      done()
    })
    it('should unpublish the schedule', function(done){
      done()
    })
  })
})
